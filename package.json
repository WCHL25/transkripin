{
  "name": "transkripin",
  "description": "Transcribes audio/video into accurate text, summarizes it into clear points, and makes content searchable and shareable with multi-language support for greater accessibility",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "prebuild": "npm run prebuild --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present"
  },
  "type": "module",
  "workspaces": [
    "frontend"
  ]
}
